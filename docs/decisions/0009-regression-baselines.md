# ADR 0009 — Immutable Regression Baselines

## Status

Accepted.

A successful verification can create a SecurityBaseline containing the verified property,
snapshot, relevant attack paths, controls, permissions, identities, agent/MCP capabilities,
evidence, and regression tests. Future results append new comparisons; the baseline itself is
not mutated.
